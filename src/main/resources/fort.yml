# ===================================================================
# Fort configuration
# ===================================================================

# ===================================================================
# Standard Fort properties.
# Full reference is available at:
# http://fort.com/conf/common-application-properties.html
# ===================================================================

fort:
    app:
        server-base: http://localhost:8080
        app-key: ppvssdiwes3b
        app-secret: rja3imicjdy6
    authentication:
        unauthorized-return: /unauthorized.html
        login:
            url: /fort/login
            success-return: /index.html
            error-return: /login.html?error=1
            login-view: /login.html
        logout:
            url: /fort/logout
            success-return: /index.html
    user:
        default-roles: user
        default-groups: dev