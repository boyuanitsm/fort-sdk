# ===================================================================
# Fort configuration
# ===================================================================

# ===================================================================
# Standard Fort properties.
# Full reference is available at:
# http://fort.com/conf/common-application-properties.html
# ===================================================================

fort:
    app:
        server-base: http://localhost:8080
        app-key: soqcnpjhxqku
        app-secret: 6ddeh9nrgtjd
    authentication:
        login:
            url: /fort/login
            success-return: /home
            error-return: /login?error_msg=1
            login-view: /login
        logout:
            url: /fort/logout
            success-return: /home
    user:
        default-roles: user
        default-groups: dev